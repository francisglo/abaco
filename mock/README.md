Mock backend para desarrollo usando `json-server`.

Endpoints principales (ejemplo):

- `GET /zones`
- `GET /neighborhoods`
- `GET /sectors`
- `GET /pollingStations`
- `GET /voters`
- `GET /actions`
- `GET /logs`
- `GET /geojson_features`

Arranque:

```powershell
npm run mock:server
```
